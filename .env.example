# Database — SQLite locally. Swap to Postgres in production:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/renewal_leak?schema=public"
DATABASE_URL="file:./dev.db"

# Stripe Connect (https://dashboard.stripe.com/settings/connect)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_CLIENT_ID="ca_..."
STRIPE_REDIRECT_URI="http://localhost:3000/api/stripe/callback"

NEXT_PUBLIC_APP_URL="http://localhost:3000"
